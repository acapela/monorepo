CREATE TABLE "public"."jira_account" ("account_id" uuid NOT NULL, "jira_cloud_id" text NOT NULL, "rest_req_last_used_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("account_id","jira_cloud_id") , FOREIGN KEY ("account_id") REFERENCES "public"."account"("id") ON UPDATE cascade ON DELETE cascade);COMMENT ON TABLE "public"."jira_account" IS E'jira_cloud_id <> account_id join table';
