import { number } from "./utils/number";

export const spacing = {
  // eg. pop menu items, sidebar topics, setting item title + subtitle spacing, topic title + subtitle spacing
  close: number(3),
  // eg. messages, settings items
  regular: number(20),
  // eg. message composer buttons, top corner user avatar + "..." button
  horizontalActions: number(10),
  horizontalActionsSection: number(15),
  pageSections: number(30),
};
