We usually track events by defining them by part of action object.

Sometimes tho, it is tricky (eg. single action modifying many items).

In such cases it might be worth doing directly where mutation happens.
