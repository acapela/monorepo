import React from "react";

interface Props {
  value: string;
  onChange: (value: string) => void;
}

export const SpaceNameInput = ({ value, onChange }: Props) => {
  return <p>SpaceNameInput will be here</p>;
};
