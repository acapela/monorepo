{
  "name": "@acapela/db",
  "version": "0.1.0",
  "scripts": {
    "introspect": "dotenv -e ../.env prisma introspect",
    "generate": "dotenv -e ../.env prisma generate"
  },
  "devDependencies": {
    "dotenv-cli": "^4.0.0",
    "prisma": "^2.17.0"
  },
  "dependencies": {
    "@acapela/config": "0.1.0",
    "@acapela/shared": "0.1.0",
    "@prisma/client": "^2.17.0"
  }
}
